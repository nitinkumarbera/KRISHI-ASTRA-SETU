# =====================================================
# Krishi Astra Setu — SERVER Environment Variables
# =====================================================
# Copy this file to .env and fill in the values.
# Set all these on Railway as environment variables.

# MongoDB Atlas connection string
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/krishiastrasetu?retryWrites=true&w=majority

# JWT secret (use a long random string)
JWT_SECRET=your_super_secret_jwt_key_here

# Allowed client origin (your Vercel URL)
CLIENT_URL=https://your-project.vercel.app

# Cloudinary credentials (for image uploads)
CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud_name
CLOUDINARY_API_KEY=your_cloudinary_api_key
CLOUDINARY_API_SECRET=your_cloudinary_api_secret

# Server port (Railway sets this automatically)
PORT=5000
